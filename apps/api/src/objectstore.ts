/**
 * Re-export from the shared storage package so both apps/api and
 * apps/index-worker use the same driver contract and implementations.
 * (closure-2 GAP A: moved into @ui-intelligence/storage.)
 */
export * from "@ui-intelligence/storage";
