# R2 Completion Matrix — requirement → evidence (audited)

Statuses: `VERIFIED` (executed evidence), `INCOMPLETE`, `BLOCKED` (external input needed), `DEFERRED`.
Audit corrections (2026-09-25) applied — see notes inline.

Status values: `done` (executed evidence exists), `partial`, `todo`.
Companion to docs/r2-plan.md. R1 matrix: docs/completion-matrix.md (all done).

## A. Framework expansion (Next.js)
| Requirement | Status | Evidence |
| --- | --- | --- |
| Second web framework (Next.js) runs the same protocol contracts, runtime-core, react adapter, renderers | VERIFIED | `apps/next-demo` (Next 15.5.26 App Router, `output: "export"`): `src/contracts.ts` redeclares the SAME `catalog.productChooser` + `ui.primaryButton` entity keys/allowed representations as the Vite reference app; kernel = `@ui-intelligence/runtime-core` RuntimeKernel + `registerAllRenderers`, boundaries via `@ui-intelligence/react` UiBoundary with `@ui-intelligence/renderers` components; host-owned live bindings (`product.open` → details drawer, `ui.action` → acknowledgment). `npx next build` succeeds (static export, route `/` prerendered, / 21.2 kB). 4 vitest tests in `apps/next-demo/test/app.test.tsx` pass (chooser renders demo products, data-ui-entity attributes on both boundaries, drawer opens through the live action, button boundary renders + invokes). |
| Capture works unchanged against the Next.js app | VERIFIED | Served the exported `out/` on http://localhost:3100 (tiny node static server): `curl` HTTP 200, page HTML contains `data-ui-entity="catalog.productChooser"` (data-ui-instance="catalog.main") and `data-ui-entity="ui.primaryButton"` (data-ui-instance="ui.toggle"), plus current product names and button label — the exact DOM contract `packages/capture` ScenarioRunner records, so capture runs unchanged against a plain static server. |

## B. Device synchronization
| Requirement | Status | Evidence |
| --- | --- | --- |
| Server sync endpoint merges device bundles by highest revision | INCOMPLETE | audit: revision-number merge has no causal meaning — divergent unequal-revision edits overwrite silently; causal base-revision model fix in flight | `POST /v1/profiles/:profileId/sync` (apps/api/src/routes/sync.ts + src/sync.ts); `synced_preferences` table in apps/api/src/schema.ts and infra/dev/schema.sql; one-transaction merge — apps/api/test/sync.test.ts ("merges device bundles…" asserts classification + authoritative state + table rows) |
| Client SyncManager drains outbox, pushes, pulls, applies | VERIFIED | packages/preferences/src/sync.ts `SyncManager.syncNow` (export → enqueueSync before push → push → clearSync after success → applyAuthoritative) + `startAutoSync`; packages/preferences/test/sync.test.ts ("pushes the local bundle, enqueues the outbox before the push…", "startAutoSync pushes on the interval until stopped") |
| Conflict policy: no silent overwrite | INCOMPLETE | equal-revision conflicts retained as (non-persistent) drafts; DIVERGENT unequal-revision edits still overwrite silently (audit finding 5); persistent drafts + causal model fix in flight | Server marks equal-revision/different-digest keys serverWins+retainedAsDraft (apps/api/src/sync.ts); client keeps the server record in the store and the local spec in `SyncManager.drafts` (documented in-memory Map); tests: apps/api/test/sync.test.ts + packages/preferences/test/sync.test.ts ("divergent equal-revision retained as draft and NOT silently applied" assertions) |
| Two devices converge | INCOMPLETE | convergence tested for non-divergent cases; the audit's divergent-edit scenario loses data silently — fix in flight; SyncManager also not wired into the app UI | packages/preferences/test/sync.test.ts ("converges two stores: device 2's higher revision is accepted, device 1 pulls it back") — after both syncs, both stores hold revision-2 chooser + the server's button spec; apps/api/test/sync.test.ts exercises the same flow through the HTTP endpoint |

## C. Persistent semantic rules
| Requirement | Status | Evidence |
| --- | --- | --- |
| Versioned rule schema validated against contracts | VERIFIED | `packages/protocol/src/rules.ts` (`semanticRuleSchema`, version 1, representation non-empty, properties record); stores reject malformed rules with `SCHEMA_INVALID` on write (`packages/preferences/test/rules.test.ts`, 12 tests); rule properties additionally validated against the renderer property schema at evaluation (`packages/runtime-core/test/rules.test.ts`) |
| RuleEngine: preference > rule > default precedence | VERIFIED | `packages/runtime-core/src/rules.ts` — `evaluate` returns the last matching enabled rule (later rules win) whose representation is contract-allowed and whose properties conform to the registry schema; `RuleEngine.resolveRepresentation` implements explicit preference > rule > contract default. Evidence: `packages/runtime-core/test/rules.test.ts` (17 tests: matching, disabled skip, nonconforming skip, later-rule-wins, precedence); reference-app hook `useResolvedRepresentation` applies it for both product chooser instances and the account export button |
| Rules stored per profile, exportable, editable in UI | VERIFIED | `getRules`/`putRules` on `PreferenceStore` (memory + IndexedDB) storing the whole list as ONE record under reserved scopeKey `__rules__` (scope `app`) — travels in `exportBundle`/`importBundle` (`packages/preferences/test/rules.test.ts` round-trip test); editor "rules" tab with validated dropdowns (entity from app contracts, representation from `allowedRepresentations`, viewport/route, compact toggle for `button.compact@1`), enable/disable + delete (`apps/reference-app/src/editor/RulesTab.tsx`; testids `rules-tab`, `rule-create`, `rule-list`, `rule-item`, `rule-delete`) |
| Browser evidence: rule drives compact on mobile; explicit preference overrides rule | VERIFIED | `apps/reference-app/test/rules.test.tsx` (jsdom, 4 tests): matchMedia stub → mobile; rule "compact on mobile for ui.primaryButton" created via the Rules tab renders the export button compact with NO explicit preference; accepting a `button.default@1` preference in the editor overrides the rule; undo returns to compact (rule); disabling the rule reverts to the contract default; desktop viewport unaffected |

## D. Managed historical runners
| Requirement | Status | Evidence |
| --- | --- | --- |
| runner-manager service schedules capture sets | VERIFIED | apps/runner-manager (Fastify + SQLite `RUNNER_DB`): POST /v1/runs → 202 {runId}, GET /v1/runs/:id with per-scenario results, worker register/heartbeat/claim, lease-checked idempotent complete. 9 unit tests green (`npx vitest run apps/runner-manager/test/manager.test.ts`). |
| Bounded worker pool with retries/heartbeats | VERIFIED | `runPool(count, opts)` spawns capture-runner children (RUNNER_MANAGER_URL/APP_URL env) with restart-on-death, or embedded in-process workers for tests; workers heartbeat every 10s, one run per worker, lease expiry / worker death re-queues the run (tests: re-claim after expiry, dead-worker re-queue, bounded retries 3 attempts, embedded pool of 2 drains 2 runs). |
| CLI `history submit` | VERIFIED | packages/cli `history submit --repo --commit --scenarios [--manager]`: POSTs the run, prints runId, polls to terminal, optional --out. Unit tests with a fetch stub verify argv parsing + request shape (packages/cli/test/submit.test.ts, 4 green). |
| End-to-end run with reference app + fixture commits | VERIFIED* | Durable publication is now implemented (audit fix): after `ScenarioRunner.execute`, both the manager executor and the managed capture-runner path upload artifacts+manifest via `CaptureUploader` to the history API and VERIFY publication (capture retrievable, occurrences > 0, artifact bytes readable from the raw endpoint) before reporting `captured` with the REAL captureId + artifactId; an idempotency conflict (spec 11 dedup) resolves to the existing verified capture; upload failure ⇒ scenario `failed`. Commit binding is now HONEST (closure audit fix 3d): a LOCAL `repoUrl` reconstructs the actual commit via `reconstructCommit` (worktree → serve → capture → publish → verify, provenance `reconstructed`); remote/absent `repoUrl` captures APP_URL with a buildArtifactDigest derived from the served page's first response HTML (never the constant "runner-managed") and records `{mode:"live-app", note:"commit binding asserted, not verified — remote/absent repoUrl"}` in the run result (persisted with the run; apps/runner-manager/test/executor.test.ts + apps/capture-runner/test/managed-publication.test.ts, 8 tests green). Regenerated docs/coverage-report.json via the LIVE reconstruction run against the dev API on :8787: 156 planned slots (13 commits × 12 scenario-recipes = 6 named scenarios × 2 viewports, closure audit fix 3c) → 144 captured with real capture ids + sha256 digests, 12 expected failures (commit 12 × 12 recipes), 0 not captured; the account scenarios capture the real account page (route no longer rewritten to "/"). |

## E. Developer handoff → PR
| Requirement | Status | Evidence |
| --- | --- | --- |
| CLI writes accepted spec into app-owned config file | VERIFIED | packages/cli handoff command (handoff.ts): accepted proposal → ui-intelligence.preferences.json entry with provenance; 5 unit tests incl. merge-by-scopeKey and no-candidate failure |
| App boots defaults from config (app defaults < org < personal precedence) | VERIFIED | PreferenceService.loadAppDefaults fetches the file and fills ONLY gaps (explicit personal preference wins by the active.get check); public/ scaffold serves it |
| Branch + commit + PR created | VERIFIED | live PR #3 | LIVE 2026-09-25: real accepted proposal proposal_cbcf351caf484f83bb3fe239 → branch vorflux/handoff/... → commit → PR https://github.com/rahulsamgoz/Future-UI/pull/3 |

## F. Per-user auth
| Requirement | Status | Evidence |
| --- | --- | --- |
| Users, hashed API keys, project memberships, roles | VERIFIED | authz.ts: users + api_keys (SHA-256 hashed, plaintext shown once) + project_members (owner/member/viewer) tables; operator token remains full owner (back-compat); POST/GET /v1/users provisioning (operator-only) |
| Role enforcement (owner/member/viewer) on all routes | done | preHandler gate in server.ts (project-scoped GET/OPTIONS ≥ viewer, mutations ≥ member; project creation + user provisioning operator-only) PLUS per-route enforcement on routes without :p (audit fix): GET /v1/artifacts/:id and /v1/artifacts/:id/raw require viewer membership resolved from the artifact ROW and PUT /v1/artifacts/:slotId requires member on the slot's project (a project hint never substitutes for membership); POST /v1/profiles/:profileId/sync binds profiles to the owning principal (new sync_profiles table, auto-registered at first sync, operator exempt) and requires ≥ member for pushed preferences / ≥ viewer for pulls; GET /v1/projects is membership-filtered (operator sees all). Proven by the finding-1 regression tests below |
| Cross-user denial tests | done | auth.test.ts: user with membership only in project A denied 403 on project B ("no membership"); viewer denied writes ("below required member"); member denied provisioning; audit-fix regressions ("finding 1" describe block): zero-membership user 403 on artifact metadata, raw bytes, PUT slot and sync read/write; viewer reads artifacts 200 but sync write 403; foreign-profile sync (write and pull) 403 while operator may access any profile; two users cannot read each other's profiles; GET /v1/projects filtered to memberships |

## G. Remote storage + retention/GC
| Requirement | Status | Evidence |
| --- | --- | --- |
| StorageDriver interface, fs + s3 drivers | done | apps/api/src/objectstore.ts: `StorageDriver` (put/get/delete/exists), `FsStorageDriver` (R1 digest-sharded layout preserved), `S3StorageDriver` (@aws-sdk/client-s3, `{bucket, prefix, client}` with client injectable, env `UI_INTEL_S3_BUCKET`), `createStorageDriver` factory on `UI_INTEL_STORAGE_DRIVER` (fs default; missing bucket → logged fs fallback). Audit fix: the driver contract is ASYNC end to end and the s3 driver issues REAL SDK commands — GetObject (Body buffered, NoSuchKey → null), HeadObject (NotFound → false), awaited Put/Delete with errors propagating — and the per-process write-through cache is gone, so bytes written by one instance are readable/deletable by another. Durable publication: the upload route awaits `store.put` before marking the artifact ready (failure → 500 with the store error, slot left open), and capture ingest verifies artifact bytes exist via `store.exists` (unuploaded artifact referenced by a capture → 422). Tests: apps/api/test/objectstore.test.ts (two instances over one fake transport: put via A → get/exists via B succeed, delete via B hides the object from A; failed PutObject rejects; key prefixing, fs layout, factory fallback) and apps/api/test/storage-durability.test.ts (failed PutObject → route 500, slot stays open, a healthy instance over the same DB fills the slot; capture ingest 422 on unuploaded artifact bytes, accepted after upload) |
| Reference-aware GC deletes orphans, keeps referenced | done | apps/api/src/gc.ts async `runGc` (byte deletion awaited BEFORE row removal; deletion errors propagate; + `POST /v1/projects/:p/gc` in src/routes/gc.ts, scheduled daily pass in apps/index-worker/src/gc.ts gated by `GC_RETENTION_DAYS` + `gc_runs`). Audit fix: the former "skip scheduled GC when UI_INTEL_STORAGE_DRIVER=s3" behavior is REMOVED — the worker now deletes bytes through the real S3 path (awaited DeleteObjectCommands, prefix + R1 sharded layout) when s3 is configured, or the fs layout otherwise. Tests: apps/api/test/gc.test.ts (old-referenced deleted + bytes gone, fresh-referenced kept, old orphan deleted, fresh orphan kept, dryRun no-op, >50% guard aborts unless forced, route-level test) and apps/index-worker/test/gc.test.ts (daily gating, fs byte deletion, s3 DeleteObjectCommand path with bucket+prefixed key, guard) |
| Retention policy honored (originals + derived) | VERIFIED | Retention window (days) is the single policy input on both the API route (`retentionDays` body or `GC_RETENTION_DAYS`) and the worker; deletion removes rows AND object bytes so derived previews sharing an artifact row share fate; artifacts of the current build's captures and any reference inside the window are never deleted (tested: "keeps artifacts whose latest reference is within retention…", "old reference on the CURRENT build → kept"). Dev-profile note: no derived embedding/preview rows exist yet (spec 14 "embedding_spaces/embeddings" tables are post-R1); the artifact-level rule already covers them |

## H. Cloud deployment artifacts
| Requirement | Status | Evidence |
| --- | --- | --- |
| Dockerfiles + compose build successfully | VERIFIED | all 5 images built; compose stack healthy (smoke: health, /v1 proxy, run submission). Note: compose starts zero capture workers — browser-equipped workers are external | Multi-stage node:22-slim Dockerfiles for api / index-worker / runner-manager (HEALTHCHECK /health) and nginx:alpine images for studio + reference-app (nginx.conf with /v1 proxy to api); root .dockerignore; docker-compose.yml (shared network, data volume, restart policies, depends_on healthy). Built all five in the sandbox: api/index-worker/runner-manager 481MB each, studio 93.8MB, reference-app 94MB. Smoke-tested containers: api + manager /health and bearer-authenticated /v1 calls, POST /v1/runs → 202, studio + reference-app SPA served with /v1 proxied to api. Full `docker compose up` verified (all 5 services healthy; index-worker needed infra/dev/schema.sql copied into its image). |
| CI workflow (tests + capture template) | INCOMPLETE | ci.yml has now RUN GREEN on main (run 36080134328); capture.yml is manual-dispatch with zero recorded runs — not yet exercised | .github/workflows/ci.yml (push/PR → node 22, npm ci, npx tsc -b, npx vitest run without UI_INTEL_E2E). .github/workflows/capture.yml (workflow_dispatch inputs repoUrl/commitSha/scenarios → starts api + manager with RUNNER_POOL_COUNT=2 + reference app, npx playwright install chromium, CLI history submit, coverage report, uploads run-result.json + coverage-report.json). YAML parsed and validated locally (python3 -c yaml.safe_load); the workflows have not run on GitHub from this sandbox — capture.yml is a template. |
